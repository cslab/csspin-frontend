# spinfile used to test the jsconfig plugin against the latest released states of
# the required plugin-packages.
spin:
    index_url: https://packages.contact.de/tools/stable
plugin_packages:
    - spin_python
    - ../../..
plugins:
    - spin_frontend.jsconfig
node:
    use: node
    mirror: https://nodejs-proxy.ops.contact.de/dist/
python:
    use: python
